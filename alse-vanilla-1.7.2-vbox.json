{
  "builders": [
    {
      "type": "virtualbox-ovf",
      "source_path": "c:\\Arseniy\\Ansible+Vagrant\\images\\alse-vanilla-1.7.2-virtualbox-mg7.2.0.ova",
      "output_directory": "c:\\Arseniy\\Ansible+Vagrant\\images2\\",
      "vm_name": "alse-vanilla-1.7.2-virtualbox-mg7.2.0-{{isotime \"2006-01-02_03-04-05\"}}",
      "format": "ova",
      "ssh_username": "{{user `user`}}",
      "ssh_password": "{{user `pass`}}",
	  "ssh_timeout": "1m",
	  "boot_wait": "25s",
	  "ssh_host": "127.0.0.1",
	  "ssh_pty": "true",
      "shutdown_command": "echo '{{user `pass`}}' | sudo -S /sbin/halt -h -p"
	
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "inline": [
        "sudo useradd vagrant -m -G $(more /etc/group | grep astra | cut -d ':' -f 1 | paste -sd ',' -) -p satrF5fwANvrQ"
      ]
    }
  ],
 
 "variables" : {
    "user": "astra",
    "pass": "astra",
    "memory": "4096",
    "cpus": "2"
   }
  
}